model Agendamento {
  id        String   @id @default(cuid())
  cliente   String
  servico   String
  data      String
  horario   String
  status    String   @default("PENDENTE")
  valor     Float
  createdAt DateTime @default(now())

  @@unique([data, horario])
}